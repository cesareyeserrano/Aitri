/**
 * Aitri v2 — programmatic API
 * For use in scripts or CI pipelines that import Aitri as a module.
 */
export { PHASE_DEFS } from './phases/index.js';
export { loadConfig, saveConfig, readArtifact } from './state.js';
