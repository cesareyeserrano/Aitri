# Test Cases: proof-integrity

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: quick.
> QA rule: Run smoke + core functional checks first, then expand abuse/edge coverage after discovery refinement.
> Quality profile: CLI/Automation (cli).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-1
- AC: Given a contract that contains `return { ok: true, fr: "FR-1", input }` without any `input.` property access, when `aitri prove --feature <name>` runs, then that FR appears as `trivial_contract` and proof `ok` is `false`.

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-1, AC-3
- AC: Given a contract that contains `return { ok: true, fr: "FR-1", input }` without any `input.` property access, when `aitri prove --feature <name>` runs, then that FR appears as `trivial_contract` and proof `ok` is `false`.

### TC-3
- Title: Validate us-3 primary behavior.
- Trace: US-3, FR-3, AC-4
- AC: Given `aitri contractgen --feature <name>` runs, when the Developer persona task is printed, then the output explicitly states that contracts returning `ok: true` without reading `input` properties are invalid.

### TC-4
- Title: Validate us-4 primary behavior.
- Trace: US-4, FR-4, AC-5
- AC: Given `aitri prove` completes with trivial contracts present, when output is displayed, then trivial contract FRs appear under a distinct `[TRIVIAL CONTRACT]` label separate from proven/unproven sections.

## Negative / Abuse

### TC-5
- Title: Handle edge behavior - Contract reads `input` but only to pass it through (e.g., `return { ok: true, fr: "FR-1", input }`) — must still be flagged as trivial (passing `input` whole is not verifying a property).
- Trace: US-1, FR-1
- AC: Given contract reads `input` but only to pass it through (e.g., `return { ok: true, fr: "fr-1", input }`) — must still be flagged as trivial (passing `input` whole is not verifying a property), when the workflow executes, then the system returns a deterministic failure-safe response.

### TC-6
- Title: Handle edge behavior - Contract conditionally returns `ok: true` based on `input.someProperty` — must NOT be flagged as trivial.
- Trace: US-1, FR-1
- AC: Given contract conditionally returns `ok: true` based on `input.someproperty` — must not be flagged as trivial, when the workflow executes, then the system returns a deterministic failure-safe response.

## Security

### TC-7
- Title: Enforce security control - Contract file content is read and regex-tested only — no execution during heuristic check.
- Trace: US-1, FR-1
- AC: Given threat scenario: contract file content is read and regex-tested only — no execution during heuristic check, when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

## Edge Cases

1. Contract reads `input` but only to pass it through (e.g., `return { ok: true, fr: "FR-1", input }`) — must still be flagged as trivial (passing `input` whole is not verifying a property)
2. Contract conditionally returns `ok: true` based on `input.someProperty` — must NOT be flagged as trivial
