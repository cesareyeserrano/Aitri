# Test Cases: audit-refactor

> Generated by `aitri plan`.
> Spec-driven rule: every test MUST trace to a User Story (US-*) and one or more Functional Rules (FR-*). Include AC-* when applicable.
> Discovery rigor profile: quick.
> QA rule: Run smoke + core functional checks first, then expand abuse/edge coverage after discovery refinement.
> Quality profile: CLI/Automation (cli).

## Functional

### TC-1
- Title: Validate us-1 primary behavior.
- Trace: US-1, FR-1, AC-6
- AC: Given a project with no `package.json`, when `aitri audit` runs, then Layer 3 skips with a "(no package.

### TC-2
- Title: Validate us-2 primary behavior.
- Trace: US-2, FR-2, AC-1
- AC: Given a project where `src/` exists but contains only `.js` files and Go source lives in `internal/`, when `aitri audit` runs, then `collectSourceFiles` includes files from `internal/` in addition to `src/`.

### TC-3
- Title: Validate us-3 primary behavior.
- Trace: US-3, FR-3, AC-4, AC-5
- AC: Given a project with AI configured, when `aitri audit` runs, then Layer 4 outputs persona system prompts and task descriptions for each applicable persona instead of calling any external API.

### TC-4
- Title: Validate us-4 primary behavior.
- Trace: US-4, FR-4, AC-3
- AC: Given a project with no `--feature` argument, when `aitri audit` runs, then the output includes "pipeline compliance skipped" and Layer 2 + Layer 3 findings are shown.

### TC-5
- Title: Validate us-5 primary behavior.
- Trace: US-5, FR-5, AC-5
- AC: Given `aitri audit --feature <name>` with an approved spec, when audit runs, then Layer 1 pipeline findings appear before Layer 2–4 output, and Layer 4 prompt includes the feature spec as context.

## Negative / Abuse

### TC-6
- Title: Handle edge behavior - Project has `src/` directory containing only contract stubs (`.js`) and real source is in `internal/` or `cmd/` (Go project) — scanner must reach both.
- Trace: US-1, FR-1
- AC: Given project has `src/` directory containing only contract stubs (`.js`) and real source is in `internal/` or `cmd/` (go project) — scanner must reach both, when the workflow executes, then the system returns a deterministic failure-safe response.

### TC-7
- Title: Handle edge behavior - Project has no `package.json` — Layer 3 (npm audit) must skip gracefully without error.
- Trace: US-1, FR-1
- AC: Given project has no `package.json` — layer 3 (npm audit) must skip gracefully without error, when the workflow executes, then the system returns a deterministic failure-safe response.

## Security

### TC-8
- Title: Enforce security control - `collectSourceFiles` directory walk must have depth and file count limits to prevent runaway scan on large monorepos (existing limits: depth 4, maxFiles 60 — preserve these).
- Trace: US-1, FR-1
- AC: Given threat scenario: `collectsourcefiles` directory walk must have depth and file count limits to prevent runaway scan on large monorepos (existing limits: depth 4, maxfiles 60 — preserve these), when an invalid or abusive action is attempted, then access is blocked and evidence is logged.

## Edge Cases

1. Project has `src/` directory containing only contract stubs (`.js`) and real source is in `internal/` or `cmd/` (Go project) — scanner must reach both
2. Project has no `package.json` — Layer 3 (npm audit) must skip gracefully without error
